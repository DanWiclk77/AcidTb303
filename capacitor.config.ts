import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.acid303.pro',
  appName: 'Acid-303 Pro',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
